{
  "version": 2,
  "installCommand": "yarn install --frozen-lockfile",
  "buildCommand": "yarn workspace @ohif/app build:viewer",
  "outputDirectory": "platform/app/dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
